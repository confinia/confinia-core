# Documents business PRIVÉS (plan, modèle financier, interviews) — jamais dans
# le repo : il passera public à la beta. Ne retirer sous aucun prétexte.
business/

# Python
__pycache__/
*.py[cod]
.venv/
venv/

# Data files — source downloads and generated outputs stay out of git
data/
*.geojson
*.gpkg
*.shp
*.shx
*.dbf
*.prj
*.cpg
*.7z
*.zip
*.csv

# OS / editor
.DS_Store
.idea/
.vscode/

# Local env
.env
# Secrets (mot de passe admin Grafana…) — jamais dans le repo
deploy/secrets.env

# Captured HTTP archives may contain session cookies — never commit
*.har
